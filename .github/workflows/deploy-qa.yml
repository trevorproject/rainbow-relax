name: Deploy to qa
on:
  workflow_dispatch:


jobs:
  deploy: 
    name: deploy to qa 
    uses: ./.github/workflows/deployment.yml
    with:
        enviroment: github-pages-qa
        path: qa
    



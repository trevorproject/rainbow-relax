name: Deploy to prod
on:
  push:
    tags:
      - prod-**


jobs:
  deploy: 
    name: deploy to prod


    uses: ./.github/workflows/deployment.yml
    with:
        enviroment: github-pages-prod
    
    
        path: prod
    
    
    